{
  "$schema": "https://context7.com/schema/context7.json",
  "projectTitle": "SecretSync",
  "description": "Go secret synchronization pipeline with a published Python binding and GitHub Action.",
  "folders": ["docs", "examples", "pkg", "cmd", "python"],
  "excludeFolders": ["docs/dist", "node_modules", ".github"],
  "excludeFiles": ["CHANGELOG.md"],
  "rules": [
    "Use the Go module github.com/jbcom/secrets-sync.",
    "The published Python distribution is secrets-sync-python-binding and imports as secrets_sync.",
    "Do not add credentials to configuration or examples; use the documented secret-provider integrations."
  ],
  "previousVersions": [
    { "tag": "v2.4.2" }
  ]
}
