# Source definitions for test secrets
# Represents Vault KV2 mounts that serve as secret sources

imports:
  - data-engineers    # Team-specific configs
  - Staging           # Inherited target
  - analytics         # Analytics platform secrets
