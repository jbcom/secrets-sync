---
title: pkg/circuitbreaker
description: Go API reference for the circuitbreaker package.
---

<!-- Code generated by gomarkdoc. DO NOT EDIT -->

# circuitbreaker

```go
import "github.com/jbcom/secrets-sync/pkg/circuitbreaker"
```

Package circuitbreaker provides circuit breaker pattern implementation for external API calls.

This package wraps external API calls to Vault, AWS services, and other external systems with circuit breaker pattern to:

- Prevent cascade failures when downstream services are degraded
- Fail fast when services are unavailable
- Provide graceful degradation
- Auto\-recover when services become healthy

## Index

- [func ExecuteTyped\[T any\]\(cb \*CircuitBreaker, ctx context.Context, fn func\(context.Context\) \(T, error\)\) \(T, error\)](<#ExecuteTyped>)
- [func WrapError\(err error, cbName string, state gobreaker.State\) error](<#WrapError>)
- [type CircuitBreaker](<#CircuitBreaker>)
  - [func New\(cfg \*Config\) \*CircuitBreaker](<#New>)
  - [func \(cb \*CircuitBreaker\) Counts\(\) gobreaker.Counts](<#CircuitBreaker.Counts>)
  - [func \(cb \*CircuitBreaker\) Execute\(ctx context.Context, fn func\(context.Context\) \(any, error\)\) \(any, error\)](<#CircuitBreaker.Execute>)
  - [func \(cb \*CircuitBreaker\) Name\(\) string](<#CircuitBreaker.Name>)
  - [func \(cb \*CircuitBreaker\) State\(\) gobreaker.State](<#CircuitBreaker.State>)
- [type Config](<#Config>)
  - [func DefaultConfig\(name string\) \*Config](<#DefaultConfig>)


<a name="ExecuteTyped"></a>
## func [ExecuteTyped](<https://github.com/jbcom/secrets-sync/blob/main/pkg/circuitbreaker/circuitbreaker.go#L171>)

```go
func ExecuteTyped[T any](cb *CircuitBreaker, ctx context.Context, fn func(context.Context) (T, error)) (T, error)
```

ExecuteTyped is a type\-safe wrapper around Execute

<a name="WrapError"></a>
## func [WrapError](<https://github.com/jbcom/secrets-sync/blob/main/pkg/circuitbreaker/circuitbreaker.go#L184>)

```go
func WrapError(err error, cbName string, state gobreaker.State) error
```

WrapError wraps an error with circuit breaker information Uses errors.Is to properly detect wrapped circuit breaker errors

<a name="CircuitBreaker"></a>
## type [CircuitBreaker](<https://github.com/jbcom/secrets-sync/blob/main/pkg/circuitbreaker/circuitbreaker.go#L61-L64>)

CircuitBreaker wraps gobreaker with observability

```go
type CircuitBreaker struct {
    // contains filtered or unexported fields
}
```

<a name="New"></a>
### func [New](<https://github.com/jbcom/secrets-sync/blob/main/pkg/circuitbreaker/circuitbreaker.go#L67>)

```go
func New(cfg *Config) *CircuitBreaker
```

New creates a new circuit breaker with the given configuration

<a name="CircuitBreaker.Counts"></a>
### func \(\*CircuitBreaker\) [Counts](<https://github.com/jbcom/secrets-sync/blob/main/pkg/circuitbreaker/circuitbreaker.go#L161>)

```go
func (cb *CircuitBreaker) Counts() gobreaker.Counts
```

Counts returns counters of the circuit breaker

<a name="CircuitBreaker.Execute"></a>
### func \(\*CircuitBreaker\) [Execute](<https://github.com/jbcom/secrets-sync/blob/main/pkg/circuitbreaker/circuitbreaker.go#L123>)

```go
func (cb *CircuitBreaker) Execute(ctx context.Context, fn func(context.Context) (any, error)) (any, error)
```

Execute wraps a function call with circuit breaker pattern

<a name="CircuitBreaker.Name"></a>
### func \(\*CircuitBreaker\) [Name](<https://github.com/jbcom/secrets-sync/blob/main/pkg/circuitbreaker/circuitbreaker.go#L166>)

```go
func (cb *CircuitBreaker) Name() string
```

Name returns the name of the circuit breaker

<a name="CircuitBreaker.State"></a>
### func \(\*CircuitBreaker\) [State](<https://github.com/jbcom/secrets-sync/blob/main/pkg/circuitbreaker/circuitbreaker.go#L156>)

```go
func (cb *CircuitBreaker) State() gobreaker.State
```

State returns the current state of the circuit breaker

<a name="Config"></a>
## type [Config](<https://github.com/jbcom/secrets-sync/blob/main/pkg/circuitbreaker/circuitbreaker.go#L22-L44>)

Config holds circuit breaker configuration

```go
type Config struct {
    // Name is the circuit breaker name (for logging and metrics)
    Name string

    // MaxRequests is the maximum number of requests allowed to pass through
    // when the circuit breaker is half-open. Default: 1
    MaxRequests uint32

    // Interval is the cyclic period of the closed state for the circuit breaker
    // to clear the internal counts. If Interval is 0, the circuit breaker doesn't
    // clear internal counts during the closed state. Default: 60 seconds
    Interval time.Duration

    // Timeout is the period of the open state after which the state becomes half-open.
    // If Timeout is 0, the timeout value is set to 60 seconds. Default: 30 seconds
    Timeout time.Duration

    // ReadyToTrip is called with a copy of Counts whenever a request fails in the closed state.
    // If ReadyToTrip returns true, the circuit breaker will be placed into the open state.
    // If ReadyToTrip is nil, default ReadyToTrip is used.
    // Default ReadyToTrip returns true when the number of consecutive failures is more than 5.
    ReadyToTrip func(counts gobreaker.Counts) bool
}
```

<a name="DefaultConfig"></a>
### func [DefaultConfig](<https://github.com/jbcom/secrets-sync/blob/main/pkg/circuitbreaker/circuitbreaker.go#L47>)

```go
func DefaultConfig(name string) *Config
```

DefaultConfig returns a circuit breaker config with sensible defaults

Generated by [gomarkdoc](<https://github.com/princjef/gomarkdoc>)
