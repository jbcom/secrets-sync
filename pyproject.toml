[dependency-groups]
dev = [
    "secrets-sync-bridge[dev]",
    "tox>=4.11",
    "tox-uv>=1.11",
]

[tool.uv.sources]
secrets-sync-bridge = { workspace = true }

[tool.uv]
workspace = { members = ["packages/secrets-sync-bridge"] }
required-version = ">=0.5.0"
